% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{estban_read}
\alias{estban_read}
\title{Read an ESTBAN CSV file}
\usage{
estban_read(path, uf = NULL, clean_names = TRUE, ref = NULL)
}
\arguments{
\item{path}{Path to a CSV downloaded with \code{\link[=estban_download]{estban_download()}} (or any
file with the same layout).}

\item{uf}{Optional character vector of two-letter state codes to keep
(e.g. \code{"PE"} or \code{c("PE", "PB")}). \code{NULL} keeps every state.}

\item{clean_names}{Logical. Convert column names to \code{snake_case} with
\code{\link[=estban_clean_names]{estban_clean_names()}} (default \code{TRUE}). With \code{FALSE} the original
upper-case names are kept, including the leading \verb{#} of \verb{#DATA_BASE}.}

\item{ref}{Optional reference month to record in the \code{ref} column. When
\code{NULL} it is taken from the \code{DATA_BASE} column of the file.}
}
\value{
A tibble with a leading integer column \code{ref} (\code{AAAAMM}), the
identification columns and one numeric column per account.
}
\description{
Reads a monthly ESTBAN file as published by the Central Bank: two title
lines to skip, \verb{;} as separator, \code{Latin-1} encoding, one row per bank
branch (\code{level = "agencia"}) or per institution and municipality
(\code{level = "municipio"}). Account columns (\verb{VERBETE_*}) are returned as
doubles in Brazilian reais; codes are kept as character to preserve
leading zeros.
}
\examples{
# A small real extract shipped with the package (PE and PB, January 2024)
f <- system.file("extdata", "202401_ESTBAN_AG_sample.CSV", package = "estbanr")
x <- estban_read(f)
x[, 1:8]

# Keep one state and the original column names
pe <- estban_read(f, uf = "PE", clean_names = FALSE)
names(pe)[1:10]
}
