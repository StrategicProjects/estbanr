% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{estban_cache_dir}
\alias{estban_cache_dir}
\title{Resolve the estbanr cache directory}
\usage{
estban_cache_dir(cache_dir = NULL, create = TRUE)
}
\arguments{
\item{cache_dir}{Optional directory path. When \code{NULL}, the resolution
order above applies.}

\item{create}{Logical. Create the directory when it does not exist
(default \code{TRUE}).}
}
\value{
A normalized directory path (character scalar).
}
\description{
The directory where downloaded ESTBAN files are kept. Resolution order:
}
\details{
\enumerate{
\item the \code{cache_dir} argument;
\item the \code{ESTBANR_CACHE_DIR} environment variable;
\item the \code{estbanr.cache_dir} R option;
\item a session-scoped folder under \code{\link[base:tempdir]{base::tempdir()}} (the default, so the
package never writes outside the temporary directory unless you opt
in).
}

Set one of the persistent options (2 or 3) to keep files across sessions
and avoid re-downloading months you already have.
}
\examples{
estban_cache_dir()

# Persistent cache for the current session only:
old <- options(estbanr.cache_dir = file.path(tempdir(), "estban-cache"))
estban_cache_dir()
options(old)
}
