% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nonreport.R
\name{estban_by_municipality}
\alias{estban_by_municipality}
\title{Aggregate ESTBAN to the municipality}
\usage{
estban_by_municipality(df, impute = TRUE, long = FALSE)
}
\arguments{
\item{df}{A tibble from \code{\link[=estban_read]{estban_read()}} or \code{\link[=estban_fetch]{estban_fetch()}} covering
several months (interpolation needs neighbours). The \code{nonreport}
column is computed when absent.}

\item{impute}{Logical. Interpolate non-reports before summing (default
\code{TRUE}). With \code{FALSE} the raw rows are summed as published.}

\item{long}{Logical. Return one row per municipality, month and account
(\code{verbete}, \code{value}) instead of one column per account (default
\code{FALSE}).}
}
\value{
A tibble keyed by \code{uf}, \code{codmun_ibge}, \code{municipio} and \code{ref}.
In wide form the account columns follow; in long form the columns
\code{verbete} (clean column name) and \code{value} follow.
}
\description{
Sums every account over the institutions (and branches) of each
municipality and month. With \code{impute = TRUE} (the default) non-reported
institution-months are interpolated first with
\code{\link[=estban_impute_nonreport]{estban_impute_nonreport()}}, so the municipal series does not collapse
when a bank is missing from a file.
}
\examples{
f <- system.file("extdata", "202401_ESTBAN_AG_sample.CSV", package = "estbanr")
x <- estban_read(f)
m <- estban_by_municipality(x, impute = FALSE)
m[, c("uf", "municipio", "ref", "verbete_160_operacoes_de_credito",
      "verbete_420_depositos_de_poupanca")]

head(estban_by_municipality(x, impute = FALSE, long = TRUE))
}
