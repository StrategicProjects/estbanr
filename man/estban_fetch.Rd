% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{estban_fetch}
\alias{estban_fetch}
\title{Download and read a range of months}
\usage{
estban_fetch(
  start,
  end = start,
  uf = NULL,
  level = c("agencia", "municipio"),
  cache_dir = NULL,
  clean_names = TRUE,
  force = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{start, end}{Reference months as \code{AAAAMM}; \code{end} defaults to \code{start}.}

\item{uf}{Optional character vector of two-letter state codes to keep
(e.g. \code{"PE"} or \code{c("PE", "PB")}). \code{NULL} keeps every state.}

\item{level}{\code{"agencia"} (one row per bank branch, the default) or
\code{"municipio"} (one row per institution and municipality).}

\item{cache_dir}{Directory for downloaded files. See \code{\link[=estban_cache_dir]{estban_cache_dir()}}
for the resolution order; the default is a session folder under
\code{\link[base:tempdir]{base::tempdir()}}.}

\item{clean_names}{Logical. Convert column names to \code{snake_case} with
\code{\link[=estban_clean_names]{estban_clean_names()}} (default \code{TRUE}). With \code{FALSE} the original
upper-case names are kept, including the leading \verb{#} of \verb{#DATA_BASE}.}

\item{force}{Logical. Re-download even when the CSV is already cached.}

\item{verbose}{Logical. Emit progress messages (default \code{TRUE}).}
}
\value{
A tibble with all requested months stacked (see \code{\link[=estban_read]{estban_read()}}).
}
\description{
Convenience wrapper around \code{\link[=estban_download]{estban_download()}} and \code{\link[=estban_read]{estban_read()}} for a
range of reference months. Months that are not published are skipped
with a warning, so the result may cover fewer months than requested;
check \code{unique(result$ref)}.
}
\examples{
\donttest{
# Needs network access; about 2 MB per month.
x <- tryCatch(estban_fetch(202401, 202403, uf = "PE", verbose = FALSE),
              error = function(e) NULL)
if (!is.null(x)) table(x$ref)
}
}
