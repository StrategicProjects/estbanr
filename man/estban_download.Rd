% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{estban_download}
\alias{estban_download}
\title{Download one ESTBAN month}
\usage{
estban_download(
  ref,
  level = c("agencia", "municipio"),
  cache_dir = NULL,
  force = FALSE,
  timeout = 300,
  verbose = TRUE
)
}
\arguments{
\item{ref}{Reference month as \code{AAAAMM} (integer, character or \code{Date}).}

\item{level}{\code{"agencia"} (one row per bank branch, the default) or
\code{"municipio"} (one row per institution and municipality).}

\item{cache_dir}{Directory for downloaded files. See \code{\link[=estban_cache_dir]{estban_cache_dir()}}
for the resolution order; the default is a session folder under
\code{\link[base:tempdir]{base::tempdir()}}.}

\item{force}{Logical. Re-download even when the CSV is already cached.}

\item{timeout}{Seconds allowed for one download (default 300).}

\item{verbose}{Logical. Emit progress messages (default \code{TRUE}).}
}
\value{
The path to the cached CSV (invisibly \code{NULL} when the month is
not published under any of the known names, with a warning).
}
\description{
Downloads the monthly file for \code{ref}, trying each name variant from
\code{\link[=estban_url]{estban_url()}}, unzips it when needed and returns the path to the CSV in
the cache directory. Months already in the cache are not downloaded
again unless \code{force = TRUE}.
}
\examples{
\donttest{
# Needs network access to www.bcb.gov.br (about 2 MB); skipped when offline.
csv <- tryCatch(estban_download(202401, verbose = FALSE),
                error = function(e) NULL)
if (!is.null(csv)) {
  pe <- estban_read(csv, uf = "PE")
  nrow(pe)
}
}
}
