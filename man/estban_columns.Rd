% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verbetes.R
\name{estban_columns}
\alias{estban_columns}
\alias{estban_verbetes}
\title{ESTBAN column layout and account dictionary}
\usage{
estban_columns(level = c("agencia", "municipio"))

estban_verbetes(level = c("agencia", "municipio"))
}
\arguments{
\item{level}{\code{"agencia"} (default) or \code{"municipio"}.}
}
\value{
A tibble. \code{estban_columns()} has \code{position}, \code{column} (original
name), \code{name} (clean name) and \code{kind} (\code{"id"} or \code{"verbete"}).
\code{estban_verbetes()} has \code{column}, \code{name}, \code{codes} (character, codes
separated by \verb{;}), \code{code} (first code, integer), \code{n_codes}, \code{side}
(\code{"ativo"} for codes below 400, \code{"passivo"} otherwise) and
\code{description}.
}
\description{
\code{estban_columns()} lists the columns of the monthly file for a level, in
file order, with their \code{snake_case} names. \code{estban_verbetes()} restricts
the list to the balance-sheet accounts (\verb{VERBETE_*}), parsing the
three-digit 'COSIF' codes out of the column name. Some columns combine
several accounts (for instance \code{VERBETE_141 + VERBETE_142}); those have
more than one code in \code{codes} and \code{n_codes > 1}.
}
\details{
The dictionary is built from the January 2024 files (see
\code{data-raw/verbetes.R} in the source repository) and covers both levels,
which share the same accounts.
}
\examples{
estban_columns()[1:8, ]
v <- estban_verbetes()
v[v$code \%in\% c(160, 420, 432), c("name", "codes", "side")]
# Combined columns
v[v$n_codes > 1, c("codes", "n_codes")]
}
