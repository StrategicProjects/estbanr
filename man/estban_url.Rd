% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{estban_url}
\alias{estban_url}
\title{Candidate download URLs for one ESTBAN month}
\usage{
estban_url(ref, level = c("agencia", "municipio"))
}
\arguments{
\item{ref}{Reference month as \code{AAAAMM} (integer, character or \code{Date}).}

\item{level}{\code{"agencia"} (one row per bank branch, the default) or
\code{"municipio"} (one row per institution and municipality).}
}
\value{
A character vector of URLs, most likely first.
}
\description{
The Central Bank has changed the file naming over the years, so for a
given month more than one name may exist. The candidates are tried in
order by \code{\link[=estban_download]{estban_download()}}; the first that answers \code{200} wins.
}
\examples{
estban_url(202401)
estban_url("202401", level = "municipio")
}
